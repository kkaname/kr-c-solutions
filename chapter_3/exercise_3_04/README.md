# Exercise 3-04: itoa

Solution to Exercise 3-04 from *The C Programming Language* (K&R).

**Question:** In a two's complement number representation, our version of itoa does not handle the largest negative number, that is, the value of n equal to -(2^(wordsize-1)). Explain why not. Modify it to print that value correctly, regardless of the machine on which it runs.



## Build & Run

### Compile:
```sh
gcc -o itoa itoa.c
```

### Run:
```sh
./itoa
```

## Expected Output:
```
```
